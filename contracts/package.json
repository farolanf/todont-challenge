{
  "name": "hardhat-project",
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "export-abi": "tsx scripts/export-abi.ts ../web/abi.json",
    "deploy": "hardhat run scripts/deploy.ts"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "dotenv": "^16.3.1",
    "hardhat": "^2.19.3",
    "tsx": "^4.7.0"
  }
}